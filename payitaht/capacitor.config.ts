import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.ashina.payitaht',
  appName: 'Payitaht',
  webDir: 'out',
  backgroundColor: '#142f2b',
  ios: { contentInset: 'never', backgroundColor: '#142f2b' },
  android: { backgroundColor: '#142f2b' },
}
export default config
